# 变更日志

## [0.4.0] - 2026-09-19

### 变更

- **peer 下限抬到 `ice-render@^2.18.0`**：那一版是**事件系统改版**（事件沿组件树冒泡、
  `ICEEvent` 的 W3C 方法真生效、事件名类型化）。本包编排出来的图最终由引擎绘制与交互，
  事件语义必须与渲染语义同版。

  仍停在 2.14~2.17 的宿主装本版本会得到 npm 的 peer 冲突提示 —— 有意的。

本文件记录所有值得注意的变更，版本号遵循语义化版本。

## [0.3.0] - 2026-09-19

### 变更

- **peer 下限抬到 `ice-render@^2.17.0`**：那一版把 `zIndex` 的默认值改成 **`'auto'` 哨兵**
  （排序当 0、同层平手按**加入顺序**；显式正数是"应用自己钉的浮层"），补了四个 z 序 API，
  并把**序列化格式升到 v2**（旧文档载入时自动迁移，绘制次序逐项不变）。

  仍停在 2.14 / 2.15 / 2.16 的宿主装本版本会得到 npm 的 peer 冲突提示 —— 有意的：
  宁可响亮失败，也不要让它在"新旧两套叠放/序列化语义"之间看着能跑、行为却对不上。

## [0.2.0] - 2026-09-18

### 变更

- **peer 下限抬到 `ice-render@^2.16.0`**：本包编排出来的图最终由引擎绘制，2.16.0 修的是「连线横穿图面里的其他图元」。

  仍停在 2.14 / 2.15 的宿主装本版本会得到 npm 的 peer 冲突提示 —— 有意的：
  宁可响亮失败，也不要让它悄悄用老路由画出一堆穿线。

## 0.1.8 - 2026-09-18

### 修复

- **`fitViewport` 在 `options.dpr > 1` 时把内容放大并裁掉**（不报错）。视口尺寸原先取
  `ice.canvasWidth / canvasHeight`，而那是 **backing store** 尺寸（= css × dpr）；渲染视口还会
  再乘一次 dpr，等于**多乘一次** —— 普通屏完全正常，只在高分屏上复发，所以能躺很久。

  现在优先用引擎的输入矩形（`ice.getInputRect()`：内容盒 = CSS 尺寸，与命中测试 / 坐标换算
  同一口径），拿不到布局信息时退回 `canvasWidth / dpr`。**明确不再退回 `canvasWidth` 本身。**

  这是"同一份文档、同一块画布，只改 dpr，取景必须逐字段一致"的口径：
  回归是**真实浏览器**的 e2e（`e2e/fit-viewport-dpr.spec.ts`），用页面里的独立预言机重算期望值，
  并且另有一条"内容四角真的落在画布 CSS 区域内"。两条都验证过**对着旧实现会红**
  （实测旧实现 scale 0.0875 vs 正确 0.525，内容跑到 x=1350 而画布只有 800）。

## 0.1.7 - 2026-09-17

### 变更

- **两个示例页改成家族统一的「一页一个类」写法**（`core-dsl.html` / `orchestration.html`）。
  构造期建好 DOM 引用与事件：`core-dsl` 的渲染动作收成 `render()`；`orchestration` 的 DSL 变成
  `static` 常量、编排控制器收进实例。两页都没有"随数据变化"的部分，所以按约定不加 `onUpdate()`。
  示例页不在 npm 包里（`files: ["dist"]`），**不含运行时功能变更**。
- 门禁：types:check ✅、jest 34/34 ✅、build ✅、真机 e2e 2/2 ✅；两页画布截图前后 sha256 完全相同。

## 0.1.6 - 2026-09-15

### 变更

- **dev 依赖对齐 `ice-render ^2.12.0`**（peer 范围不变，仍是 `^2.10.0`）。本包只产出场景数据、不碰引擎显示树；
  引擎 2.12.0 含 2.11.3 的连线命中语义修正，对本包是空操作 —— **不含功能变更**。
- 门禁（在 2.12.0 上重跑）：types:check ✅、jest 34/34 ✅、build ✅、示例页 e2e 2/2 + 冒烟 2/2 ✅。

## 0.1.5 - 2026-09-15

### 变更

- **dev 依赖对齐 `ice-render ^2.11.2`**（peer 范围不变，仍是 `^2.10.0`）。本包只产出场景数据、不碰引擎显示树，
  引擎这次改的是「对齐引导的候选目标范围」，对本包是空操作 —— **不含功能变更**。
- 门禁（在 2.11.2 上重跑）：types:check ✅、jest ✅、build ✅、示例页 e2e ✅。

## 0.1.4 - 2026-09-15

### 变更

- **peer / dev 依赖对齐 `ice-render ^2.10.0`**（原 `^2.8.0`）。本包产出场景数据、不碰引擎显示树，
  因此只需要声明范围对齐；在 2.10.1 上重跑了门禁（tsc + build + jest 34/34 + 示例页 e2e 2/2）。

## 0.1.3 - 2026-09-15

### 变更

- **peer / dev 依赖对齐 `ice-render ^2.8.0`**：引擎 2.8.0 起布局机制对齐 Swing
  （不继承 / 自顶向下校验 / 尺寸协商 / 快照往返）。本包产出的是场景数据、不碰引擎显示树，
  仅声明范围对齐。

### 验证

- `npm run verify`（tsc + jest 34/34 + build）、`npm run test:e2e` 2/2。

## [0.1.0] - 2026-09-14

### 新增

- **DSL 里声明主题**：文档新增顶层 `theme` 字段 —— 字符串走命名主题（内置 `default` / `dark`，
  或应用层 `registerTheme` 注册的名字），对象按**部分主题**深合并（平铺 `{ "primary": "#0d6efd" }`、
  显式分层 `{ "semantic": {...} }`、深层局部 `{ "motion": { "duration": { "fast": 50 } } }` 都支持）。
  `renderDsl()` 在建图元之前应用它（preset 与默认样式都是构造时按主题展开的）。
- 图元样式里可以直接引用主题 token：`"style": { "fillStyle": "$primary", "strokeStyle": "$chrome.slot.fill" }`
  —— 引擎在绘制那一刻解析，所以换主题时整张图跟着换。

### 诊断（给 agent 自修复用）

- `ICE_DSL_THEME_NAME_UNKNOWN`：写了没注册的命名主题（引擎会**静默回退 default**，这是最容易踩的坑），
  诊断里会列出当前可用的名字；
- `ICE_DSL_THEME_TOKEN_UNKNOWN`：样式里的 `"$xxx"` 在生效主题里解析不出来
  （引擎遇到会**跳过赋值** —— 视觉上就是"颜色没生效但不报错"），路径精确到 `nodes[0].style.fillStyle`；
- `ICE_DSL_THEME_INVALID`：`theme` 写成了数组 / 数字这类非法形态。

### 依赖

- `ice-render` peer / dev 提到 `^2.4.0`（`theme` 字段与 token 引用是 2.4 的能力）。

### 验证

- 单测 28 → **34**（命名主题已注册/未注册、三种部分主题写法、非法形态、节点与连线的 token 引用检查）。

## [0.0.9] - 2026-09-14

- 依赖对齐 ice-render 2.4.0；SKILL 补《Theming（引擎 2.4 起）》一节（版本 1.1.0 → 1.2.0）。

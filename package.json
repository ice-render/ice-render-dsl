{
  "name": "ice-render-dsl",
  "version": "0.4.4",
  "description": "A JSON-first DSL layer for AI agents to drive ice-render.",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ice-render/ice-render-dsl.git"
  },
  "main": "dist/index.cjs",
  "module": "dist/index.mjs",
  "browser": "dist/index.umd.js",
  "types": "dist/types/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "npm run clean && npm run build:types && npm run build:js",
    "build:js": "rollup -c --environment NODE_ENV:production",
    "build:types": "tsc --emitDeclarationOnly",
    "clean": "rimraf dist",
    "dev": "rollup -c -w --environment NODE_ENV:development",
    "test": "jest --runInBand",
    "types:check": "tsc --noEmit",
    "test:e2e": "npm run build && playwright test",
    "verify": "npm run types:check && npm test && npm run build",
    "verify:full": "npm run verify && npm run test:e2e"
  },
  "devDependencies": {
    "@babel/core": "7.28.5",
    "@babel/preset-env": "7.28.5",
    "@babel/preset-typescript": "7.28.5",
    "@playwright/test": "^1.63.0",
    "@rollup/plugin-babel": "6.1.0",
    "@rollup/plugin-commonjs": "28.0.1",
    "@rollup/plugin-json": "6.1.0",
    "@rollup/plugin-node-resolve": "15.3.1",
    "@rollup/plugin-terser": "0.4.4",
    "@types/jest": "29.5.14",
    "@types/node": "18.19.130",
    "babel-jest": "29.7.0",
    "http-server": "^14.1.1",
    "ice-render": "^4.2.0",
    "jest": "29.7.0",
    "rimraf": "5.0.10",
    "rollup": "3.29.5",
    "typescript": "5.9.3"
  },
  "engines": {
    "node": ">=18",
    "npm": ">=9"
  },
  "peerDependencies": {
    "ice-render": "^4.2.0"
  }
}

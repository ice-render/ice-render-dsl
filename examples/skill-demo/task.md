# Task

Use the `ice-render-dsl` skill to generate a small architecture diagram.

Requirements:

- Three nodes:
  - Browser
  - DSL Runtime
  - Canvas
- One directed edge from Browser to DSL Runtime
- One directed edge from DSL Runtime to Canvas
- Use orthogonal edge routing

Return only a valid JSON DSL document.
